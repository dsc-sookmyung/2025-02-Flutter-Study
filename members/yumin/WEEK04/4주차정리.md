# 🪗 Xylophone

## 🎼 Packages

- 패키지를 삽입해 Flutter 앱에서 외부 기능(예: 오디오 재생, 데이터 처리 등) 사용 가능
- pubspec.yaml 파일에 패키지 이름과 버전을 추가한 후, flutter pub get 명령으로 설치
- 이후 import 'package:패키지명/패키지명.dart'; 구문을 통해 코드에서 해당 기능 불러오기
- 이번 프로젝트에서 사용한 패키지 링크: https://pub.dev/packages/audioplayers/install

## 🎹 buildKey() 함수

- buildKey() 함수는 재사용 가능한 위젯 빌더 함수로, 같은 구조의 버튼을 색과 음만 바꿔서 반복 생성함
- required 키워드를 사용해 null-safety를 보장

## 🎵 xylophone.dart 코드

```dart
import 'package:flutter/material.dart';
import 'package:audioplayers/audioplayers.dart';

void main() => runApp(XylophoneApp());

class XylophoneApp extends StatelessWidget {
  XylophoneApp({super.key});

  void playSound(int soundNumber) async {
    final player = AudioPlayer(); 
    await player.play(AssetSource('note$soundNumber.wav'));
  }

  Expanded buildKey({required Color color, required int soundNumber}) {
    return Expanded(
      child: Container(              
        color: color,
        child: TextButton(
          onPressed: () => playSound(soundNumber),
          child: const SizedBox.expand(), 
          style: TextButton.styleFrom(
            foregroundColor: Colors.white, 
            shape: const RoundedRectangleBorder(),
          ),
        ),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        backgroundColor: Colors.black,
        body: SafeArea(
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.stretch,
            children: <Widget>[
              buildKey(color: Colors.red, soundNumber: 1),
              buildKey(color: Colors.orange, soundNumber: 2),
              buildKey(color: Colors.yellow, soundNumber: 3),
              buildKey(color: Colors.green, soundNumber: 4),
              buildKey(color: Colors.teal, soundNumber: 5),
              buildKey(color: Colors.blue, soundNumber: 6),
              buildKey(color: Colors.purple, soundNumber: 7),
            ],
          ),
        ),
      ),
    );
  }
}
```
