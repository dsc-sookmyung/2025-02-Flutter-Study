# 전하은- 4주차 정리

## 9. xylophone

---

- **Flutter package** : 다른사람이 만든 코드의 오픈 소스 라이브러리
  - 패키지 각각의 점수를 보면 가장 적합한 걸 고를 수 있다.
  - pubspec.yaml에 추가 -> pub get

```dart
english_words: ^4.0.0 // 패키지이름 + 버전정보
```

```dart
import 'package:audioplayers/audioplayers.dart'; // 오디오플레이어패키지
final player = AudioCache();
                player.play('note1.wav'); // 전체경로를 쓸 필요없음
```

---

- **함수**  
1) type 함수이름 (type + input) 
2) void 함수이름 (type + input) {}  
    - void = 출력이 없다

---

- **=>** : {} + return
  -   한 줄 표현만 가능

```dart
void main() => runApp(const Xylophone());
```