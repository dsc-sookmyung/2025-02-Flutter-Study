# Clima

### API (Application Programming Interface, 응용프로그램 인터페이스)
- geolocator api

Flutter에서 기기의 위치 정보(GPS) 를 가져올 때 사용하는 패키지

- accuracy (정확도 설정)

```
LocationAccuracy.low, // accuracy 값이 높을 수록 배터리가 많이 든다.
```

### 권한(permissions) 설정
- Android

경로 : android > app > src > main > AndroidManifest.xml

```
<uses-permission android:name="android permission.ACCESS_COARSE_LOCATION" />

<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> //안 되면 여기까지 맨 위에 추가
```

- iOS

경로 : ios > Runner > Info.plist

```
<key>NSLocationWhenInUseUsageDescription</key><br><string>현재 위치 기반으로 날씨 정보를 제공합니다.</string>
```

### async & await
- Synchronous Programming (동기 프로그래밍)

한 작업이 끝난 이후(대기) 다음 작업 실행 가능

- Asynchronous Programming (비동기 프로그래밍)

시간이 걸리는 작업을 백그라운드에서 병렬로 처리하여 앱이 멈추지 않도록 함
🔸 await는 비동기 함수가 끝날 때까지 기다렸다가 결과를 반환하도록 한다.

```
void getLocation() async {

    final LocationSettings locationSettings = LocationSettings(
      accuracy: LocationAccuracy.low,
      distanceFilter: 100,
    );

    Position position = await Geolocator.getCurrentPosition(locationSettings: locationSettings);
    print(position);
}
```

### Future

미래에 완료될 작업의 결과를 표현하는 객체
- 시간이 걸리는 작업(예: 위치 정보, 네트워크 요청, 파일 읽기 등)을 지금 즉시 처리하지 않고, 나중에 완료될 것임을 표현
- 아직 결과가 없더라도 Future 객체는 즉시 반환됨 → 앱이 멈추지 않음


### Lifecycle Widget (StatefulWidget의 생명주기)

- initState() : 위젯이 처음 생성될 때 호출
- deactivate() : 위젯이 파괴될 때 호출
- build() : 위젯이 빌드될 때(그려질 때) 호출

### 오류 처리 (Error Handling)

-try-catch 문을 사용하여 예외 발생 시 프로그램이 멈추지 않게 처리한다.

```
try {
  // 시도할 코드
} catch (e) {
  print(e);
}
```

### Null Aware Operator

null일 수도 있는 값을 안전하게 처리할 때 사용

```someVariable ?? defaultValue```

- ?? → 앞의 값이 null이면 뒤의 기본값(defaultValue)을 사용

### HTTP Status codes (상태 코드)

- 200 : OK (성공)
- 4xx : error (클라이언트 오류)

```
if (response.statusCode == 200) {
String data = response.body;
} else {
print(response.statusCode);
}
```