# 내 프로필 만들기

---

# 사용한 추가 위젯 및 함수

## 전체 구조 개요

Scaffold를 기본 틀로 두고,

- 상단에는 **AppBar**
- 본문은 **SingleChildScrollView**로 감싼 여러 개의 **Card**로 배치함.

이때, 카드 안에서는 Stack과 ClipOval을 활용해 배경 이미지와 프로필 이미지를 겹치게 구성.

---

## 1️⃣ Color(Hex)

### 1. 개념

Color(0xAARRGGBB)에서

- AA는 투명도 (Alpha)
- RR, GG, BB는 색상

⇒ 즉, Color(0xFFFAF8F2)는 투명 효과가 전혀 없는 RGB 그대로의 색상

### 2. 코드 내 활용

- AppBar, Card, Container, BoxDecoration 등 전반적으로 일관된 톤 유지용.
- UI 전체에 따뜻한 **베이지-브라운 계열 컬러 팔레트**를 통일시켜 시각적 안정감 부여.

```dart
color: const Color(0xFFFAF8F2),

```

**프로필 카드의 배경**, **AppBar 배경**, **둥근 프로필 테두리** 등에 반복적으로 사용하였음.

---

## 2️⃣ SingleChildScrollView — 스크롤 가능한 전체 화면

### 1. 개념

- 한 화면에 담기 어려운 콘텐츠를 **스크롤 가능**하게 만드는 컨테이너.
- 내부에 **자식 위젯을 하나만 둘 수 있지만**, 보통 그 안에 Column을 넣어 여러 요소를 배치할 수 있음.

### 2. 코드 내 활용

```dart
body: SingleChildScrollView(
  child: Column(
    children: [
      Card(...),
      Container(...),
      Container(...),
    ],
  ),
),

```

- Column 안에 여러 개의 Card 섹션(프로필, Playlist, Favorite, Contact 등)을 배치함.
  - 콘텐츠 양이 많아질 경우, 화면을 넘어가도 스크롤이 가능하도록 보장.
  - 덕분에 **기기 해상도에 상관없이 레이아웃이 잘리지 않음.**

> 즉, 화면 전체를 유동적으로 구성해, 자기소개 카드들이 세로로 스크롤되며 자연스럽게 이어지는 구조 제공.

---

## 3️⃣ Stack

### 1. 개념

- 위젯들을 **위에 겹쳐서** 배치할 때 사용하는 위젯.
  - 레이어 개념처럼 “배경-전경” 순서대로 배치할 수 있음.

### 2. 코드 내 활용

```dart
Stack(
  alignment: Alignment.bottomLeft,
  children: <Widget>[
    _buildBackground(), // 배경 이미지
    Padding(
      padding: const EdgeInsets.only(left: 10.0, bottom: 10.0),
      child: _buildProfile(primaryColor, cardColor), // 프로필 이미지
    ),
  ],
),

```

- 배경 이미지 위에 둥근 프로필을 **겹쳐 표시 가능**
- alignment: Alignment.bottomLeft 덕분에 프로필이 **배경 하단 왼쪽**에 자연스럽게 위치.
- Padding으로 세밀한 여백 조정 (사진이 테두리에 붙지 않도록)

> 정적인 소개 카드에 입체감과 시각적 중심 요소(프로필) 를 부여해서, 배경 위에 떠 있는 것처럼 표현 가능

---

## 4️⃣ AppBar

: 해당 개념은 이미 배웠지만, 안에 있는 속성들을 추가해서 좀 더 서치해본 관계로 추가했음.

### 1. 개념

- Flutter의 상단 바로, 제목, 뒤로가기 아이콘, 액션 버튼 등을 포함 가능.
  - elevation 값으로 그림자 강도를 조절할 수 있고, shadowColor나 surfaceTintColor로 색 왜곡을 방지 가능

### 2. 코드 내 활용

```dart
appBar: AppBar(
  backgroundColor: const Color(0xFFFAF8F2),
  surfaceTintColor: Colors.transparent, // M3 색 왜곡 방지
  elevation: 5,
  shadowColor: Color(0x4D403329), // 그림자 투명도 조절 (0x4D = 30%)
  leading: Icon(Icons.chevron_left_rounded, color: Color(0xFF403329)),
  title: const Text(
    "Introduce Myself",
    style: TextStyle(
      color: Color(0xFF403329),
      fontFamily: 'Electrolize',
      fontSize: 20,
      letterSpacing: 1,
    ),
  ),
  centerTitle: true,
),

```

- **배경색:** 카드와 동일한 베이지 톤 (#FAF8F2)
- **elevation:** 5로 설정 → 하단에 은은한 그림자 주기 가능
- **shadowColor:** 색상 일관성 유지를 위한 반투명 브라운 그림자 배치 (0x4D403329)
- **surfaceTintColor:** Material 3의 자동 색조 효과 제거
- **centerTitle:** 제목 중앙 정렬

> 결과적으로 화면 전체의 톤앤매너를 통일하면서도, 상단에 자연스러운 시각적 계층(깊이)을 추가함.

---

## 5️⃣ ClipOval

### 1. 개념

- 사각형 형태의 이미지를 **원형 또는 타원형으로 자르는 위젯**입니다.
  - 보통 프로필 이미지, 로고 등에서 사용

### 2. 코드 내 활용

```dart
Container(
  width: size,
  height: size,
  decoration: BoxDecoration(
    shape: BoxShape.circle,
    color: const Color(0xFFFAF8F2),
    border: Border.all(color: cardColor, width: 3.0),
  ),
  child: ClipOval(
    child: Image(image: AssetImage('images/songhaeun.jpg')),
  ),
),

```

- BoxDecoration에서 shape: BoxShape.circle로 원형 영역을 만들고, 그 안에서 ClipOval로 **이미지 자체를 원형으로 자름**
  - 사진이 원에서 제대로 배치되지 않는 문제를 해결할 수 있고, 중앙에 배치되도록 할 수 있음.

> 프로필 이미지를 시각적으로 중심화하면서, 카드의 곡선 디자인과 어울리도록 부드럽게 처리.

---

## 각 위젯의 관계 구조

```
MaterialApp
 └─ Scaffold
     ├─ AppBar (상단 제목 + 그림자)
     └─ SingleChildScrollView
          └─ Column
              ├─ Card 1
              │    └─ Stack (배경 + ClipOval 프로필)
              ├─ Card 2 (Playlist)
              ├─ Card 3 (My Favorite)
              ├─ Card 4 (Least Favorite)
              └─ Card 5 (Contact List)

```

---

# 프로필 구현

## 프로필 구현 전 피그마

[https://www.figma.com/design/zWbROhp6061thImrOmfkn8/-%EC%86%A1%ED%95%98%EC%9D%80--Flutter-5%EC%A3%BC%EC%B0%A8-%EB%82%B4-%ED%94%84%EB%A1%9C%ED%95%84-%EB%A7%8C%EB%93%A4%EA%B8%B0?node-id=1-33&t=VfsYBp4tgEtBp0o3-1](https://www.figma.com/design/zWbROhp6061thImrOmfkn8/-%EC%86%A1%ED%95%98%EC%9D%80--Flutter-5%EC%A3%BC%EC%B0%A8-%EB%82%B4-%ED%94%84%EB%A1%9C%ED%95%84-%EB%A7%8C%EB%93%A4%EA%B8%B0?node-id=1-33&t=VfsYBp4tgEtBp0o3-1)

## 프로필 구현 코드

```dart
import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    const String profileAsset = 'images/songhaeun.jpg';
    const String backgroundAsset = 'images/background.png';
    const Color primaryColor = Color(0xFF403329);
    const Color primaryTransColor = Color(0x80403329);
    const Color cardColor = Color(0xFFFAF8F2);

    return MaterialApp(
      home: Scaffold(
        backgroundColor: const Color(0xFFF5F1E4),
        appBar: AppBar(
          backgroundColor: const Color(0xFFFAF8F2),
          surfaceTintColor: Colors.transparent, // M3에서 색 왜곡 방지
          elevation: 5, // 그림자 유지
          leading: Icon(Icons.chevron_left_rounded, color: Color(0xFF403329)),
          shadowColor: Color(0x4D403329), // 그림자 명확하게 보이게
          title: const Text(
            "Introduce Myself",
            style: TextStyle(
              color: const Color(0xFF403329), // 배경이 밝으니까 흰색 대신 검정
              fontFamily: 'Electrolize',
              fontSize: 20,
              letterSpacing: 1,
            ),
          ),
          centerTitle: true,
        ),
        body: SingleChildScrollView(
          child: Column(
            children: [
              Card(
                elevation: 4.0,
                shadowColor: Color(0x80403329), // 그림자 명확하게 보이게
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(15.0),
                ),
                color: cardColor, // 카드의 색상을 흰색 배경색과 통일 (선택 사항)
                margin: const EdgeInsets.fromLTRB(15.0, 20.0, 15.0, 20.0),
                child: Column(
                  mainAxisSize: MainAxisSize.min,
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: <Widget>[
                    // 1. 배경과 프로필 이미지를 겹치는 부분 (Stack)
                    Stack(
                      alignment: Alignment.bottomLeft,
                      children: <Widget>[
                        // A. 배경 이미지
                        _buildBackground(),

                        // B. 프로필 이미지 (왼쪽, 아래쪽 여백으로 위치 조정)
                        Padding(
                          padding: const EdgeInsets.only(
                            left: 10.0,
                            bottom: 10.0,
                          ),
                          child: _buildProfile(primaryColor, cardColor),
                        ),
                      ],
                    ),

                    // 2. 텍스트 정보
                    const Padding(
                      padding: EdgeInsets.fromLTRB(20.0, 20.0, 20.0, 20.0),
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          Text(
                            'EunHaSong',
                            style: TextStyle(
                              fontFamily: 'Electrolize',
                              fontSize: 20.0,
                              letterSpacing: 2.0,
                              color: primaryColor,
                              fontWeight: FontWeight.bold,
                            ),
                          ),
                          SizedBox(height: 5),
                          Text(
                            'Department of Artificial Intelligence',
                            style: TextStyle(
                              fontSize: 14,
                              color: primaryTransColor,
                              fontFamily: 'Electrolize',
                            ),
                          ),
                        ],
                      ),
                    ),
                  ],
                ),
              ),
              Container(
                width: double.infinity,
                child: Card(
                  elevation: 4.0,
                  shadowColor: Color(
                    0xFF403329,
                  ).withOpacity(0.3), // 그림자 명확하게 보이게
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(15.0),
                  ),
                  color: cardColor,
                  margin: const EdgeInsets.fromLTRB(15.0, 0.0, 15.0, 20.0),
                  child: Padding(
                    padding: const EdgeInsets.symmetric(
                      horizontal: 20.0,
                      vertical: 15.0,
                    ),
                    child: Column(
                      mainAxisSize: MainAxisSize.min,
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: <Widget>[
                        _widgetText(20.0, primaryColor, 'Playlist 🎶'),
                        _widgetText(
                          12.0,
                          primaryTransColor,
                          'Songs that heal you while listening to them',
                        ),
                        const SizedBox(height: 10.0),
                        Divider(thickness: 1, height: 1, color: primaryColor),
                        const SizedBox(height: 10.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'SPINNIN’ON IT - NIMIXX ',
                        ),
                        const SizedBox(height: 5.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'Dear, My Feelings - IVE   ',
                        ),
                        const SizedBox(height: 5.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'FOCUS - Hearts2Hearts',
                        ),
                        const SizedBox(height: 5.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'DIFFERENT - LE SSERAIM',
                        ),
                      ],
                    ),
                  ),
                ),
              ),
              Container(
                width: double.infinity,
                child: Card(
                  elevation: 4.0,
                  shadowColor: Color(
                    0xFF403329,
                  ).withOpacity(0.3), // 그림자 명확하게 보이게
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(15.0),
                  ),
                  color: cardColor,
                  margin: const EdgeInsets.fromLTRB(15.0, 0.0, 15.0, 20.0),
                  child: Padding(
                    padding: const EdgeInsets.symmetric(
                      horizontal: 20.0,
                      vertical: 15.0,
                    ),
                    child: Column(
                      mainAxisSize: MainAxisSize.min,
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: <Widget>[
                        _widgetText(20.0, primaryColor, 'My Favorite 🤎'),
                        _widgetText(
                          12.0,
                          primaryTransColor,
                          'Let’s enjoy this together!',
                        ),
                        const SizedBox(height: 10.0),
                        Divider(thickness: 1, height: 1, color: primaryColor),
                        const SizedBox(height: 10.0),
                        _widgetText(12.0, primaryColor, '지하철에서 음악 듣기'),
                        const SizedBox(height: 5.0),
                        _widgetText(12.0, primaryColor, '소설 정주행하기'),
                        const SizedBox(height: 5.0),
                        _widgetText(12.0, primaryColor, '향수 시향하기'),
                      ],
                    ),
                  ),
                ),
              ),
              Container(
                width: double.infinity,
                child: Card(
                  elevation: 4.0,
                  shadowColor: Color(
                    0xFF403329,
                  ).withOpacity(0.3), // 그림자 명확하게 보이게
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(15.0),
                  ),
                  color: cardColor,
                  margin: const EdgeInsets.fromLTRB(15.0, 0.0, 15.0, 20.0),
                  child: Padding(
                    padding: const EdgeInsets.symmetric(
                      horizontal: 20.0,
                      vertical: 15.0,
                    ),
                    child: Column(
                      mainAxisSize: MainAxisSize.min,
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: <Widget>[
                        _widgetText(20.0, primaryColor, 'My Least Favorite 🤍'),
                        _widgetText(
                          12.0,
                          primaryTransColor,
                          'Not really my thing.',
                        ),
                        const SizedBox(height: 10.0),
                        Divider(thickness: 1, height: 1, color: primaryColor),
                        const SizedBox(height: 10.0),
                        _widgetText(12.0, primaryColor, '야채를 먹는걸 싫어합니다...'),
                        const SizedBox(height: 5.0),
                        _widgetText(12.0, primaryColor, '정적을 참을 수 없어합니다...'),
                        const SizedBox(height: 5.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          '과제를 노가다하는 걸 좋아하지 않습니다...',
                        ),
                      ],
                    ),
                  ),
                ),
              ),
              Container(
                width: double.infinity,
                child: Card(
                  elevation: 4.0,
                  shadowColor: Color(
                    0xFF403329,
                  ).withOpacity(0.3), // 그림자 명확하게 보이게
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(15.0),
                  ),
                  color: cardColor,
                  margin: const EdgeInsets.fromLTRB(15.0, 0.0, 15.0, 20.0),
                  child: Padding(
                    padding: const EdgeInsets.symmetric(
                      horizontal: 20.0,
                      vertical: 15.0,
                    ),
                    child: Column(
                      mainAxisSize: MainAxisSize.min,
                      crossAxisAlignment: CrossAxisAlignment.start,
                      children: <Widget>[
                        _widgetText(20.0, primaryColor, 'Contact List 💌'),
                        _widgetText(
                          12.0,
                          primaryTransColor,
                          'Feel free to reach out here.',
                        ),
                        const SizedBox(height: 10.0),
                        Divider(thickness: 1, height: 1, color: primaryColor),
                        const SizedBox(height: 10.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'Gmail : shaeun03290@sookmyung.ac.kr',
                        ),
                        const SizedBox(height: 5.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'Instagram : @_1s.7h_5e',
                        ),
                        const SizedBox(height: 5.0),
                        _widgetText(
                          12.0,
                          primaryColor,
                          'Phone : 010-6300-5850',
                        ),
                      ],
                    ),
                  ),
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }

  Widget _buildBackground() {
    return ClipRRect(
      borderRadius: const BorderRadius.only(
        topLeft: Radius.circular(15.0),
        topRight: Radius.circular(15.0),
      ),
      child: Image(image: AssetImage('images/background.png')),
    );
  }

  Widget _buildProfile(Color primaryColor, Color cardColor) {
    const double size = 90.0;

    return Container(
      width: size,
      height: size,
      decoration: BoxDecoration(
        shape: BoxShape.circle,
        color: const Color(0xFFFAF8F2),
        border: Border.all(color: cardColor, width: 3.0),
      ),
      child: ClipOval(child: Image(image: AssetImage('images/songhaeun.jpg'))),
    );
  }

  Widget _widgetText(double fontSize, Color primaryColor, String text) {
    return Text(
      text,
      style: TextStyle(
        fontFamily: 'Electrolize',
        fontSize: fontSize,
        letterSpacing: 2.0,
        color: primaryColor,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}

```
